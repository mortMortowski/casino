//import setRedBet from '../Home.js';

function verifyBet(){
    /*if(madeBet){

        setRedBet(0);
        setGreenBet(0);
        setBlackBet(0);
        setMadeBet(false);

        if(result === 'red' && redBet > 0){
            setBalance(balance + (redBet * 2));
        }else if(result === 'green' && greenBet > 0){
            setBalance(balance + (greenBet * 14));
        }else{
            setBalance(balance + (blackBet * 2));
        }
    }*/
}

//export default verifyBet;